name: plumbing_quote_app
description: A plumbing quote app with scope-based clause auto-fill functionality.

publish_to: 'none' # Remove this line if you plan to publish to pub.dev.

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

flutter:
  uses-material-design: true