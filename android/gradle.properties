# Enable AndroidX for modern Flutter support
android.useAndroidX=true
android.enableJetifier=true

# Optimize Gradle builds
org.gradle.jvmargs=-Xmx4096M

# Kotlin Gradle Plugin version
kotlin.code.style=official

# Force disable Flutter V1 embedding (if needed)
flutter.embedding.android.v2=true
